import 'package:flutter/material.dart';

class Analysticalsscreen extends StatelessWidget {
  const Analysticalsscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}